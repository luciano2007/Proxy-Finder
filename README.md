# Proxy-Finder
Python tool that scrapes fresh free HTTP/SOCKS proxies from multiple public sites, normalizes results, and exports ready‑to‑use IP:port lists for scraping, OSINT, and lab pentesting. Old code is a blueprint for swapping in new sources, filters, and validation.
